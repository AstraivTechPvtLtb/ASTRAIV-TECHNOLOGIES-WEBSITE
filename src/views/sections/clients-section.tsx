'use client';

/**
 * @file clients-section.tsx
 * @description Enterprise client logos and metrics are now unified inside `StatsSection`
 * in accordance with the enterprise layout specification (Screenshot 2).
 * This component is kept as a safe no-op export for backwards compatibility.
 */

export function ClientsSection() {
  return null;
}
